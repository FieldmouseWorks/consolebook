[package]
name = "consolebook-server"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1"
argon2 = "0.5"
axum = "0.8"
axum-extra = { version = "0.10", features = ["cookie"] }
# Streaming the export reads rows one at a time (`TryStreamExt::try_next`)
# so a unit's bytes are released as it is written (#47).
futures-util = { version = "0.3", default-features = false, features = ["std"] }
# The streamed export's response body: an incremental body over a bounded
# channel, so delivery never buffers the archive.
http-body = "1"

clap = { version = "4", features = ["derive", "env"] }
getrandom = "0.3"
mime_guess = "2"
rust-embed = { version = "8", features = ["interpolate-folder-path"] }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = [
    "runtime-tokio",
    "sqlite",
    "macros",
    "migrate",
] }
time = { version = "0.3", features = ["formatting", "macros"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1", features = ["v4"] }
# The timezone database is always the bundled one — never the host's —
# so local-time resolution has the one-executable semantics ADR 0009
# documents, on every platform.
jiff = { version = "0.2", default-features = false, features = ["std", "tzdb-bundle-always"] }
zip = { version = "8.6.0", default-features = false }

[dev-dependencies]
http-body-util = "0.1"
tempfile = "3"
tower = { version = "0.5", features = ["util"] }

[lints]
workspace = true
